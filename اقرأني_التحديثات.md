# 🚀 دليل التحديثات السريع

## الإعداد الأولي (مرة واحدة)

### 1️⃣ إنشاء Repository في GitHub

```bash
# اذهب إلى: https://github.com/new
# اسم الـ Repo: villahadad
# النوع: Private (خاص)
```

### 2️⃣ الحصول على GitHub Token

```bash
# 1. اذهب إلى: https://github.com/settings/tokens
# 2. اضغط "Generate new token (classic)"
# 3. اختر Scope: ✅ repo (كامل)
# 4. انسخ الـ Token
```

### 3️⃣ إضافة الـ Token

```bash
# في ملف .env أضف:
GH_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx
```

### 4️⃣ تحديث electron-builder.cjs

```javascript
// غيّر YOUR_GITHUB_USERNAME باسمك:
publish: {
  provider: 'github',
  owner: 'YOUR_GITHUB_USERNAME',  // ⚠️ هنا!
  repo: 'villahadad',
  releaseType: 'release'
},
```

### 5️⃣ رفع الكود

```bash
git remote add origin https://github.com/YOUR_USERNAME/villahadad.git
git push -u origin main
```

---

## 📦 نشر تحديث جديد

### الطريقة السهلة (موصى بها)

```bash
./publish-release.sh
```

سيسألك:
- نوع التحديث (patch/minor/major)
- ملاحظات التحديث
- تأكيد

ثم يبني وينشر تلقائياً! ✅

### الطريقة اليدوية

```bash
# 1. رفع رقم الإصدار
npm version patch  # أو minor أو major

# 2. بناء التطبيق
npm run build:production

# 3. نشر إلى GitHub
npx electron-builder --mac --publish always

# 4. رفع إلى Git
git push origin main
```

---

## 🔄 كيف يعمل؟

1. **أنت تنشر** تحديث جديد → GitHub
2. **التطبيق يفحص** كل ساعة
3. **المستخدم يستلم** إشعار
4. **يضغط "تحديث"** → تنزيل تلقائي
5. **إعادة تشغيل** → تم! ✅

---

## ❓ استكشاف الأخطاء

### "GH_TOKEN not found"
```bash
# أضف الـ Token في .env
echo 'GH_TOKEN=ghp_xxxx' >> .env
```

### "Repository not found"
```bash
# تأكد من:
# 1. اسم المستخدم صحيح في electron-builder.cjs
# 2. الـ Repo موجود في GitHub
```

### التحديث ما يظهر
```bash
# تأكد من:
# 1. الـ Release موجود في GitHub
# 2. الملفات موجودة: .dmg, .zip, latest-mac.yml
```

---

## 📚 للمزيد

راجع: `GITHUB_RELEASES_GUIDE.md` للدليل الكامل

---

**بالتوفيق! 🚀**
