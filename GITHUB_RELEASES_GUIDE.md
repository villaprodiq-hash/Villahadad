# 🚀 دليل نشر التحديثات عبر GitHub Releases

هذا الدليل يشرح كيف تنشر تحديثات التطبيق باستخدام **GitHub Releases** (مجاني وسهل).

---

## 📋 المتطلبات

1. **حساب GitHub** (مجاني)
2. **Repository خاص أو عام** للتطبيق
3. **GitHub Personal Access Token** (للنشر التلقائي)

---

## 🔧 الإعداد الأولي (مرة واحدة فقط)

### الخطوة 1: إنشاء Repository في GitHub

```bash
# إذا ما عندك repository، أنشئ واحد:
# 1. اذهب إلى https://github.com/new
# 2. اسم الـ Repo: villahadad
# 3. اختر: Private (خاص) أو Public (عام)
# 4. اضغط "Create repository"
```

### الخطوة 2: ربط المشروع بـ GitHub

```bash
# في مجلد المشروع:
cd /Users/mohamedaljaff/Desktop/villahadad

# إضافة Remote (غيّر YOUR_USERNAME باسمك)
git remote add origin https://github.com/YOUR_USERNAME/villahadad.git

# رفع الكود
git push -u origin main
```

### الخطوة 3: إنشاء GitHub Token

```bash
# 1. اذهب إلى: https://github.com/settings/tokens
# 2. اضغط "Generate new token" → "Generate new token (classic)"
# 3. اسم الـ Token: "Villa Hadad Auto-Update"
# 4. اختر Scopes:
#    ✅ repo (كامل)
# 5. اضغط "Generate token"
# 6. انسخ الـ Token (يظهر مرة واحدة فقط!)
```

### الخطوة 4: حفظ الـ Token

```bash
# أضف الـ Token في ملف .env:
echo 'GH_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx' >> .env

# ⚠️ لا تشارك هذا الـ Token مع أحد!
```

### الخطوة 5: تحديث electron-builder.cjs

افتح ملف `electron-builder.cjs` وغيّر:

```javascript
publish: {
  provider: 'github',
  owner: 'YOUR_GITHUB_USERNAME', // ⚠️ غيّر هذا!
  repo: 'villahadad',
  releaseType: 'release'
},
```

---

## 📦 نشر تحديث جديد

### الطريقة 1: Script تلقائي (موصى به)

```bash
# استخدم الـ Script الجاهز:
./publish-release.sh

# سيسألك:
# - نوع التحديث (patch/minor/major)
# - رسالة التحديث
# ثم يبني وينشر تلقائياً!
```

### الطريقة 2: يدوياً

```bash
# 1. بناء التطبيق
npm run build:production

# 2. نشر إلى GitHub
npx electron-builder --mac --publish always

# ✅ تم! التحديث الآن في GitHub Releases
```

---

## 🔄 كيف يعمل Auto-Update؟

### للمستخدمين (تلقائي):

1. **التطبيق يفحص** GitHub كل ساعة
2. **إذا لقى تحديث جديد** يعرض إشعار
3. **المستخدم يضغط "تحديث"**
4. **التطبيق ينزل ويثبت** تلقائياً
5. **إعادة تشغيل** - تم!

### للمطور (أنت):

```bash
# 1. عدّل الكود
# 2. شغّل:
./publish-release.sh

# 3. انتظر 5-10 دقائق
# 4. جميع المستخدمين راح يستلمون الإشعار!
```

---

## 📝 أمثلة على التحديثات

### تحديث صغير (Bug Fix)

```bash
# مثال: إصلاح مشكلة في إضافة موظف
npm version patch  # 1.0.4 → 1.0.5
git add .
git commit -m "fix: إصلاح مشكلة إضافة موظف"
./publish-release.sh
```

### تحديث متوسط (ميزة جديدة)

```bash
# مثال: إضافة تقرير جديد
npm version minor  # 1.0.5 → 1.1.0
git add .
git commit -m "feat: إضافة تقرير المبيعات الشهري"
./publish-release.sh
```

### تحديث كبير (تغييرات جذرية)

```bash
# مثال: تصميم جديد كامل
npm version major  # 1.1.0 → 2.0.0
git add .
git commit -m "feat!: تصميم جديد كامل للتطبيق"
./publish-release.sh
```

---

## 🛠️ استكشاف الأخطاء

### خطأ: "GH_TOKEN not found"

```bash
# الحل: أضف الـ Token في .env
echo 'GH_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx' >> .env
```

### خطأ: "Repository not found"

```bash
# الحل: تأكد من:
# 1. اسم المستخدم صحيح في electron-builder.cjs
# 2. الـ Repo موجود في GitHub
# 3. الـ Token له صلاحية repo
```

### خطأ: "Release already exists"

```bash
# الحل: غيّر رقم الإصدار
npm version patch
```

### التحديث ما يظهر للمستخدمين

```bash
# تأكد من:
# 1. الـ Release في GitHub موجود
# 2. الملفات موجودة: .dmg, .zip, latest-mac.yml
# 3. المستخدمين على نفس الشبكة (أو عندهم إنترنت)
```

---

## 📊 مراقبة التحديثات

### عرض جميع الإصدارات

```bash
# في GitHub:
# اذهب إلى: https://github.com/YOUR_USERNAME/villahadad/releases
```

### إحصائيات التنزيل

```bash
# في كل Release، راح تشوف:
# - عدد التنزيلات
# - تاريخ النشر
# - الملفات المرفقة
```

---

## 🔒 الأمان

### ⚠️ لا تشارك أبداً:

- `GH_TOKEN` - خاص جداً!
- `.env` - يحتوي على مفاتيح سرية

### ✅ آمن للمشاركة:

- الكود المصدري (إذا الـ Repo عام)
- ملفات الـ Releases (.dmg, .zip)

---

## 📚 موارد إضافية

- [Electron Auto-Update Docs](https://www.electron.build/auto-update)
- [GitHub Releases Guide](https://docs.github.com/en/repositories/releasing-projects-on-github)
- [Semantic Versioning](https://semver.org/)

---

## ✅ Checklist نهائي

قبل ما تنشر أول تحديث، تأكد من:

- [ ] Repository موجود في GitHub
- [ ] GH_TOKEN مضاف في .env
- [ ] electron-builder.cjs معدّل (owner + repo)
- [ ] الكود مرفوع على GitHub (git push)
- [ ] البناء يشتغل (npm run build)

إذا كل شي ✅ → شغّل `./publish-release.sh` وانطلق! 🚀

---

**بالتوفيق! إذا عندك أي سؤال، راجع هذا الدليل أو اسأل.** 💪
