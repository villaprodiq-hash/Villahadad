---
description: Rebuild and package the application for macOS
---

# Rebuild Workflow

This workflow rebuilds and packages the Villa Hadad application for macOS.

// turbo-all

1. Navigate to project directory and run the package command:

```bash
cd "/Users/mohamedaljaff/Documents/MyApps/villahadad-v2 copy" && npm run package:mac
```

The packaged application will be available in the `release` directory.
