# تغيير التسميات: إزالة "موقع" وتوضيح سرى vs فيلا

## 🎯 الهدف
توضيح الفرق بين:
- **سرى الحداد**: المصورة التي تقدم جلسات التصوير
- **فيلا حداد**: الاستديو/الفيلا التي تُؤجر للآخرين

## ✅ التغييرات المُنجزة

### 1. ManagerAccountsView.tsx
```diff
- title="إيرادات فيلا حداد (الموقع)"
+ title="إيرادات فيلا حداد"
```

### 2. ManagerTopStatsBar.tsx
```diff
- { label: 'جلساتي', value: ... }
- { label: 'حجوزات الموقع', value: ... }
+ { label: 'جلسات سرى', value: ... }
+ { label: 'حجوزات فيلا', value: ... }
```

### 3. AddBookingModal.tsx
```diff
- <span>مناسبات الموقع</span>
+ <span>مناسبات فيلا</span>

- ? 'حجز فيلا / موقع'
+ ? 'حجز فيلا حداد'
```

### 4. ManagerPackagesView.tsx
```diff
- { id: 'venue_party', title: 'حجز الموقع بالكامل', ... }
+ { id: 'venue_party', title: 'حجز الفيلا بالكامل', ... }

- { id: 'venue', label: 'الموقع', ... }
+ { id: 'venue', label: 'فيلا حداد', ... }
```

### 5. AdminGeniusDashboard.tsx
```diff
- title={isSura ? 'حجز سرى' : 'حجز الموقع'}
+ title={isSura ? 'حجز سرى' : 'حجز فيلا'}
```

### 6. ManagerBookingDetailsView.tsx
```diff
- (booking.category === BookingCategory.LOCATION ? 'تأجير موقع' : ...)
+ (booking.category === BookingCategory.LOCATION ? 'حجز فيلا' : ...)
```


## 📊 التصنيف النهائي

### سرى الحداد (Sura)
كل أنواع التصوير التي تقوم بها سرى بنفسها:
```
┌─────────────────────────────────────────────────────┐
│  BookingCategory.WEDDING  →  زفاف                  │
│  BookingCategory.STUDIO   →  ستوديو                │
│  BookingCategory.BIRTHDAY →  أعياد ميلاد           │
│  BookingCategory.GRADUATION → تخرج                 │
│  BookingCategory.FAMILY   →  عائلي                 │
│  BookingCategory.SHOOT    →  جلسات تصوير           │
└─────────────────────────────────────────────────────┘
```

### فيلا حداد (Villa)
كل أنواع تأجير الاستديو/الفيلا:
```
┌─────────────────────────────────────────────────────┐
│  BookingCategory.LOCATION → حجز فيلا              │
│                                                      │
│  venue_party    → مناسبات فيلا                    │
│  venue_room     → غرفة VIP                        │
│  venue_session  → جلسات فيلا                      │
│  venue_commercial → إعلاني                        │
└─────────────────────────────────────────────────────┘
```


## 🔧 آلية الفلترة

```typescript
// كل حجز له category:
// - LOCATION = فيلا (تأجير)
// - أي شيء آخر = سرى (تصوير)

const isVilla = (booking.category || '').toLowerCase() === 'location';
const isSura = !isVilla;
```


## 📁 الملفات المتبقية (تحتاج مراجعة)

ملفات أخرى قد تحتوي على كلمة "موقع" تحتاج تغيير:
- `BookingCalendar.tsx` - "الموقع" (زر الموقع الجغرافي)
- `LocationPickerModal.tsx` - "الموقع المحدد" (نظام الخرائط)
- `SelectionLightbox.tsx` - "الموقع" (موقع التصوير)
- `BookingWorkflow.tsx` - "موقع خارجي" (موقع الحفل)

هذه الملفات تستخدم "موقع" بمعنى **الموقع الجغرافي** وليس تأجير الفيلا، 
فهي تحتاج قرارك: هل تبقيها كما هي أم تغيرها؟
