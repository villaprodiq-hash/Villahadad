# ============================================
# 🔐 VillaHadad Environment Configuration
# ============================================
# ⚠️ IMPORTANT SECURITY NOTES:
# 1. NEVER commit .env files to Git
# 2. Copy this file to .env and fill your values
# 3. Keep sensitive keys ONLY in .env (gitignored)
# 4. Rotate all keys if exposed
# ============================================

# Database Configuration (Local PostgreSQL)
VITE_DB_TYPE=postgres
VITE_DB_HOST=your_db_host
VITE_DB_PORT=5432
VITE_DB_USER=your_db_user
VITE_DB_PASS=your_db_password
VITE_DB_NAME=your_db_name

# Offline Mode
VITE_OFFLINE_MODE=cache

# ⚡️ SUPABASE CONFIGURATION
# Get these from: https://app.supabase.com/project/YOUR_PROJECT/settings/api
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key_here
# Public domain used to build client portal links (/select?token=...)
VITE_CLIENT_PORTAL_BASE_URL=https://select.villahadad.org

# ☁️ Cloudflare R2 (required for cloud photo upload)
# IMPORTANT: IngestionService expects R2_BUCKET_NAME (not R2_BUCKET)
R2_ACCOUNT_ID=your_r2_account_id
R2_ACCESS_KEY_ID=your_r2_access_key_id
R2_SECRET_ACCESS_KEY=your_r2_secret_access_key
R2_BUCKET_NAME=your_r2_bucket_name
# Example: https://pub-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx.r2.dev or custom domain
R2_PUBLIC_URL=https://media.villahadad.org

# 🔒 SECURITY NOTE: Service Role Key REMOVED from frontend
# The sync system now uses a secure Edge Function that has server-side access
# DO NOT add VITE_SUPABASE_SERVICE_KEY here - it exposes full database access
# The Edge Function 'sync' handles admin operations securely

# 🍎 Apple Notarization (for macOS app signing)
# Get App-Specific Password from: https://appleid.apple.com/account/manage
APPLE_ID=your_apple_id@email.com
APPLE_APP_SPECIFIC_PASSWORD=xxxx-xxxx-xxxx-xxxx
APPLE_TEAM_ID=YOUR_TEAM_ID

# 🤖 AI Configuration (Optional)
GEMINI_API_KEY=your_gemini_api_key

# 🔄 Auto-Update Configuration
# For GitHub Releases (Option 1 - Recommended)
# ⚠️ SECURITY: Token needs 'repo' scope ONLY
# Create at: https://github.com/settings/tokens/new
GH_TOKEN=your_github_personal_access_token

# For Custom Server (Option 2 - Synology NAS or Web Server)
UPDATE_SERVER_URL=https://your-server.com/updates/mac

# 💳 ZainCash Payment Gateway
# Get from ZainCash merchant dashboard
# ⚠️ NEVER commit these - configure in Supabase Edge Functions Settings
# ZC_MERCHANT_ID=your_merchant_id
# ZC_SECRET=your_secret
# ZC_MSISDN=your_msisdn
# ZC_ENV=production  # or 'test' for development

# 🔒 Security Configuration
# Generate a strong random key for session encryption
# Example: openssl rand -hex 32
STORAGE_ENCRYPTION_KEY=your_random_32_char_hex_key_here
